This directory includes some pages of the app, and each page has it's own
directory. Some common files on each directory:

`layout.dart`                       Defines what that page has.

Pages now:

`contact_list`                      The home page and the full list.