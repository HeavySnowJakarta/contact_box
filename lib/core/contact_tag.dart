// This file defines what a tag of a contact includes.

import 'package:flutter/material.dart';

class ContactTag{
  String uuid;
  String tagName;
  Color color;

  ContactTag(this.uuid, this.tagName, this.color);
}