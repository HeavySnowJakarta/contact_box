The directory that stores all of the source code.

`core`:                 Core things like how a contact object is composed.
`ui`:                   The UI for phone and tablet screens etc.
`l10n`:                 The source strings of different languages.
`generated`:            L10n strings, generated automatically.
`main.dart`:            Judge the type of the device and do the UI.